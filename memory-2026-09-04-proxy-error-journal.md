# 默认请求错误证据日志

## 用户要求与边界

用户要求遇到报错默认保留一些真实响应，不只当前 Sol 任务，不应每次先手工开启定向采集。统一日志覆盖 CCSM 公共 HTTP 转发入口及 Responses 重连入口，保留协议转换前的响应错误证据。原任务级 codex-error-capture 作为额外专项采集保留，两者不是同一个开关。

不改变 HTTP 状态、响应字节、错误类型或现有重试判决；不自动安装、重启 CCSM 或重放真实任务。本次隔离工作树基于 29df386a，主工作区 forwarder.rs 等文件有其他任务修改，合入时必须按本次增量处理，不能整文件覆盖或提交他人工作。

## 记录策略

- 默认写入应用配置目录 `logs/proxy-errors.jsonl`，两个轮转文件 `proxy-errors.1.jsonl`、`proxy-errors.2.jsonl`。每文件阈值 2 MiB，仅轮转自有固定文件名。
- 每次实际发送分配 attempt_id，尽可能关联请求头中的 UUID 任务 ID、上游 origin、上游 request-id、HTTP 状态和字节数。没有任务 ID 的请求不伪造；不同上游仅同一 origin 时仍需结合已有 router 日志定位具体供应商/模型。
- 记录 HTTP 非 2xx 的真实响应摘录、HTTP 200 JSON 错误、异常 HTML/损坏 JSON、SSE 错误事件、解析异常、无终态 EOF、传输错误，以及消费者提前停止读取的本地事实。`response_consumption_stopped` 不是上游失败判决；没有真实响应体时记录 null，不伪造正文。
- 正常对话内容不写入新日志。错误 JSON 中凭据键和 request/messages/prompt/input/output/content/headers 等回显字段被移除，再进行文本凭据遮盖。非 JSON 文本保留脱敏摘录。损坏或截断 JSON 为避免回显泄漏，保留状态/长度/摘要及省略标记，不强行保存正文。脱敏是尽力处理，错误消息仍可能包含业务文本，分享前需要人工检查。
- HTTP 正文仅在内存暂存前 64 KiB，持久化摘录最多 8000 字符；明确标记截断、解码是否可用、捕获前缀 hash。hash 不是完整原始正文 hash。压缩错误体限量解压；压缩 SSE 在结束时对限量捕获进行解码检查，超限/不支持时记录无法检查，不宣称有完整响应。
- 未压缩 SSE 分块检查，不改变转发字节；超大事件只记检查上限事实，不缓存完整流。协议未知而未发现通用终态时，仅记 EOF 观察，不据此触发新重试。
- 有界后台写入队列 128 条，日志 I/O 不阻塞请求。队列/磁盘失败后的丢失条数由后续记录的 previous_records_lost 标明；进程被强制结束可能丢失尚未落盘的记录。Unix 使用 0600，Windows 继承应用用户日志目录 ACL。
- 重连路径原本直接丢弃非 2xx 正文，改为不阻塞重试的后台捕获，最多 32 个并发、每个最多 1 秒/64 KiB。超时/超限记录部分正文和未完整读取事实；并发已满时明确记录采集跳过。不会因此增加或取消重试。

## 证据与检索

内置搜索命中 OWASP 官方 Logging Cheat Sheet，支持保存应用/连接/第三方错误证据并剔除凭据，扩展响应信息宜独立保存；Matrix 独立搜索仅返回泛化结果，不能交叉证实实现。实现判据为本地源码、实际回归测试和只读审查。未向检索服务提交任何真实任务内容或凭据。

新测试先复现漏识别错误及凭据/请求回显泄漏，随后基础 7 项通过。最终扩展验证结果在交付前补充；这里不把开发结果当作已安装程序验收。

2026-09-05 最终隔离工作树验证：`cargo test --manifest-path src-tauri/Cargo.toml --lib proxy:: -- --quiet --test-threads=4`，1867 passed / 0 failed。包括 12 项新日志回归；URL origin、无 error 的失败状态两项审查问题有明确先红后绿证据。代码只读复审通过，重连后台采集的时间/字节边界、原样字节/错误传递、gzip错误、多行SSE、轮转及脱敏均有回归。rustfmt、git diff --check 和严格 UTF-8/无 BOM 检查通过。未安装、未重启、未在真实配置目录启用新代码。

主工作区按本次补丁合入后，同一命令再次验证 1867 passed / 0 failed（2002 项被过滤）。forwarder.rs 中另一个任务的两处时区失败通知修改保持未暂存，本次仅暂存自己的公共发送入口增量；其他任务文件未纳入本次提交。主工作区新增/修改文件再次通过严格 UTF-8/无 BOM 校验。
