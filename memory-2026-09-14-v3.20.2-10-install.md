# 2026-09-14 v3.20.2-10 本地构建 + 官方事务“卸载→安装”验收（含端口残留根修）

## 用户要求

按最新 main 构建新版本，用仓库既有的安全事务脚本（先卸载旧版本再安装新版本）替换安装；不允许再手工替换文件。

## 构建

- 源码：`main@226843b7`（含 `8786bd15` 端口占用诊断/自动恢复、`1f183b3b` 协议探测证据修复、`226843b7` 版本提升）。
- 干净 detached worktree：`C:\Users\sunda\Documents\LLMservice\ccswitchmulti-build-v3.20.2-10`；`scripts/local-release-pipeline.ps1 -ReleaseRoot ...\ccswitchmulti-v3.20.2-10-local -Reason portfix-autoretry-3.20.2-10`。
- 产物：`windows/installer/CCSwitchMulti_3.20.2-10_x64-setup.exe`（SHA-256 `5E7E671B3426B34FB0BAB412342551098BBFB9EF771DFC0C2836B0343DBD6070`）、内嵌 installed payload SHA-256 `17F5AE8F51557A1BC57611EF9B9D85EA8730FB9AC2604C08AF09EAF3A3604A77`、raw exe 44,107,776 B。

## 安装前现场（上一次非事务替换的后遗症）

- 磁盘 `%LOCALAPPDATA%\CCSwitchMulti\cc-switch.exe` = **3.20.2-7**（43,997,696 B，`4BDC3C46…D6A0EF`），注册表 = 3.20.2-7，而监听进程与 `app-run-marker.json` = **3.20.2-9** → 磁盘 ≠ 运行态。
- 事务的 `Remove-CcsmVerifiedRunMarker` 要求 `marker.version == 磁盘版本`，因此会抛 “run marker does not belong to the exact stopped CCSwitchMulti process”。已用脚本自身函数做 Rehearse 复现（`run-marker-rehearsal.json`），确认 pid/path/startTicks 与监听进程完全一致、marker.version 与运行镜像一致后，才 retire 该 marker（`run-marker-retirement.json`）再走官方事务。

## 第一次事务失败（23:05–23:07，重要现场）

- 事务 stop 掉 PID 29944 后 `wait-port-release` **120 秒超时**；回滚报 `Cannot find a process with the process identifier 29944 / no process ID returned`（回滚用旧 PID 重启）→ 服务整夜不可用，直到次日 10:27 机器重启、10:29 自启才恢复。
- 回滚错误本身证明：当时 TCP 表里仍有一条 LISTEN 行指向**已经不存在的 PID 29944** —— 和应用 `PORT_OWNERSHIP_GUARD` 看到的“无法核验的监听者”是同一现象（00:12:29 新实例启动时仍复现该错误）。
- 旧实现的两个缺陷：`WaitPortReleased` 在 owner 身份读不到时 `catch { return $false }` 静默轮询到超时（日志里没有 PID）；回滚用旧 PID 重启，失败后不再拉起任何进程。

## 事务脚本根修（提交 `3ff9bfa0`）

1. `StopVerifiedProcess`：杀已验证主进程前按父 PID 采样子进程，主进程退出后一并结束（本次实测停掉 `msedgewebview2.exe` PID 33020），避免监听 socket 残留在存活子进程里。
2. `WaitPortReleased`：owner 不可读时不再静默：记录 `port-owner-unreadable`（PID、GetProcess 错误、tasklist、netstat 快照），持续 20 秒即带 PID 精确 fail-closed。
3. 端口释放后由事务自己用 `TcpListener(ExclusiveAddressUse)` 占住，直到 `StartProcess` 才释放（`port-held-during-install` / `port-hold-released`），杜绝第三方在“卸载→安装”窗口抢端口；回滚启动旧版本改为按路径启动并做有界等待，绝不再留“零进程”状态。
- 验证：`scripts/tests/install-ccswitchmulti-transaction.Tests.ps1` Pester 52/52；`Parser::ParseFile` 0 errors。

## 第二次事务成功（v3.20.2-10 已安装）

- 事务 `ccsm-20260914-113536-29920522197a499787709d78d643c928`（wrapper 根 `ccsm-20260914-113534-db866967022e49fbafdb2c7993286ea8`）事件序列：`preflight-ok(14028)` → `backup-ok` → `verified-child-stopped(msedgewebview2 33020)` → `port-held-during-install(15721)` → `port-hold-released(new)` → `transaction-success(NewPid 13704)`，总耗时 16 秒。
- 安装后独立复核：installed **3.20.2-10** / SHA-256 `17F5AE8F…4A77`（等于 installer 内嵌 payload 期望值）、注册表 3.20.2-10、监听 `127.0.0.1:15721` PID 13704、`/health` healthy、`/status` version=3.20.2-10 role=takeover、run marker=3.20.2-10 → 磁盘/运行/注册表/marker 一致，漂移消除。
- 应用日志 11:35:50 `=== CC Switch v3.20.2-10 started ===`，11:35:51 `已恢复 codex 的代理接管状态`，无 `PORT_OWNERSHIP_GUARD`。
- 二进制校验含 `占用诊断`、`retryingTakeoverRestore`、`recovery-outcome-resolved`，说明端口诊断/自动重试确实进入安装态。

## 残留边界

- `recovery-outcomes.json` 仍有 `2026-09-14 00:12:30` 的 `startupTakeoverFailed`（未确认）：本轮启动成功不会自动清掉历史条目，UI 上可手动关闭；新版本只在“端口占用→自动恢复成功”路径 resolve。
- 证据目录（仓库外）：`C:\Users\sunda\Documents\LLMservice\ccsm-portfix-acceptance-20260913\`（`pre-state/`、`install/`、`install-2/`、`transaction.jsonl`）。
- 本次未推送 GitHub、未发布 Release。