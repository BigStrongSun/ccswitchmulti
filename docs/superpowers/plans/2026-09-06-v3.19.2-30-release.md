# CCSwitchMulti v3.19.2-30 Release Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish the already integrated post-v3.19.2-29 fixes as a verified CCSwitchMulti v3.19.2-30 GitHub Release without merging unfinished worktrees.

**Architecture:** Treat the current local `main@9aedbdab` as the release source, because the completed-branch audit found no additional finished CCSwitchMulti branch outside it. Keep upstream `farion1231/cc-switch` synchronization as a separate migration: the current histories diverge by 1608 CCSwitchMulti commits and 130 upstream commits and a direct merge has broad conflicts, so it is not safe to fold into a release-only change. Perform versioning and release documentation in an isolated worktree, run all local gates, merge the release commit into `main`, then push an immutable annotated tag and verify GitHub Actions and every published updater asset.

**Tech Stack:** Git, React/TypeScript/Vitest, Rust/Cargo/Tauri, PowerShell/Pester, GitHub Actions and GitHub CLI.

**Spec:** The user requested another branch audit and, if no new completed branch remains, a new release.

## Global Constraints

- Preserve every existing dirty worktree and the untracked files in the main checkout.
- Do not merge `codex-power-presets`, `codex-multirouter-ssot-v2`, `codex-reasoning-probe-backend`, `error-journal-20260904`, or `ccsm-agent-mesh`; they remain unfinished or no-go work.
- Do not install, stop, or restart the currently running CCSwitchMulti or Codex Desktop for this release-only task.
- Keep repository text UTF-8 without BOM and without U+FFFD.
- Every local commit message body ends with `本次提交由BigStrongsSun完成`.
- Never reuse, delete, or move an existing tag.
- A pushed tag is not a completed release: all workflow jobs, public Release state, 19 assets, six updater platforms, signatures, URLs, and SHA-256 digests must be verified.

---

### Task 1: Freeze the release audit

**Files:**
- Modify: `memory.md`
- Modify: `docs/superpowers/plans/2026-09-06-v3.19.2-30-release.md`

**Interfaces:**
- Consumes: fetched local, fork, and upstream refs plus all registered worktree statuses.
- Produces: an auditable classification of completed, unfinished, superseded, excluded, and upstream-only work.

- [x] **Step 1: Fetch all remotes and tags without changing working files.**

  Run: `git fetch --all --prune --tags`

- [x] **Step 2: Compare local main, fork main, upstream main, tags, PRs, branches, and worktree dirtiness.**

  Expected: no new completed CCSwitchMulti branch is absent from local main; `codex-power-presets` has uncommitted work and stays excluded.

- [x] **Step 3: Audit upstream divergence without mutating the release tree.**

  Run: `git merge-tree --write-tree main origin/main`

  Expected: broad conflicts prove that upstream 3.20.x synchronization is a separate migration, not a release-note-only merge.

### Task 2: Create release identity and notes

**Files:**
- Modify: `package.json`
- Modify: `src-tauri/Cargo.toml`
- Modify: `src-tauri/Cargo.lock`
- Modify: `src-tauri/tauri.conf.json`
- Create: `docs/release-notes/v3.19.2-30-zh.md`

**Interfaces:**
- Consumes: `v3.19.2-29..9aedbdab` commit and file history.
- Produces: four identical version sources and a source-backed Chinese release body.

- [x] **Step 1: Change exactly the four version sources from `3.19.2-29` to `3.19.2-30`.**

  Run: `rg -n '3\.19\.2-(29|30)' package.json src-tauri/Cargo.toml src-tauri/Cargo.lock src-tauri/tauri.conf.json`

  Expected: every package version is `3.19.2-30`; dependency versions in Cargo.lock remain unchanged.

- [x] **Step 2: Write release notes covering history repair, preserved SSE errors, bounded error evidence, egress timezone monitoring, protocol evidence, and official model reasoning metadata.**

  Expected: notes distinguish source/test evidence from installed runtime and GitHub multi-platform assets.

- [x] **Step 3: Commit only release identity, notes, plan, and audit memory.**

  Run: `git diff --cached --name-only`

  Expected: no generated build artifact, user file, or unfinished branch file is staged.

### Task 3: Run local release gates

**Files:**
- Verify only: repository source and tests.

**Interfaces:**
- Consumes: the exact release commit candidate.
- Produces: fresh local pass/fail evidence before tag creation.

- [x] **Step 1: Run all frontend tests without a concurrent Rust cold build.**

  Run: `pnpm test:unit`

  Expected: 169 files and 1379 tests pass.

- [x] **Step 2: Run TypeScript, Prettier, renderer, and diff gates.**

  Run: `pnpm typecheck`, `pnpm format:check`, the renderer production build used by the formal export script, and `git diff --check`.

  Result: all passed. A separate diagnostic `pnpm build` with Tauri's default `bundle.targets=all` compiled the application but failed only in WiX `light.exe`; the release workflow and formal Windows export use NSIS, and the repository export script completed that NSIS path successfully.

- [x] **Step 3: Run complete Rust tests and static gates.**

  Run: `cargo test --manifest-path src-tauri/Cargo.toml`, `cargo check --manifest-path src-tauri/Cargo.toml --all-targets`, strict production Clippy, and rustfmt check.

- [x] **Step 4: Run Windows release/installation transaction Pester tests.**

  Expected: every repository-defined release safety test passes; no installation transaction is executed.

  Result: 90 passed, 0 failed.

- [x] **Step 5: Strictly decode all changed text as UTF-8 and reject BOM or U+FFFD.**

  Expected: zero invalid files.

### Task 4: Integrate and publish immutable source

**Files:**
- Git refs only.

**Interfaces:**
- Consumes: a fully green release commit on `bigstrongsun/release-v3.19.2-30`.
- Produces: local `main`, remote `fork/main`, and annotated `v3.19.2-30` pointing at the same commit.

- [ ] **Step 1: Fast-forward local `main` to the verified release branch without deleting any worktree.**

- [ ] **Step 2: Verify the tag name does not exist locally or remotely, then create annotated `v3.19.2-30`.**

- [ ] **Step 3: Push `main` and the tag to `fork` without force.**

- [ ] **Step 4: Verify remote peeled tag commit and `fork/main` equal the local release commit.**

### Task 5: Verify GitHub publication

**Files:**
- Modify after publication: `memory.md`

**Interfaces:**
- Consumes: GitHub CI and Release workflow runs triggered by the tag.
- Produces: public release evidence and a post-release memory commit that does not move the release tag.

- [ ] **Step 1: Wait for every CI and Release job to complete successfully.**

- [ ] **Step 2: Verify the Release is public, non-draft, non-prerelease, and Latest.**

- [ ] **Step 3: Download and verify all 19 assets and GitHub SHA-256 digests.**

- [ ] **Step 4: Verify `latest.json` has version `3.19.2-30`, exactly six supported platform keys, tag-scoped URLs, and non-empty signatures matching `.sig` assets.**

- [ ] **Step 5: Record final publication evidence in `memory.md`, commit it after the tag, and push `main` without moving the tag.**
