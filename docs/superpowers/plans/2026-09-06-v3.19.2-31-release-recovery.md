# CCSwitchMulti v3.19.2-31 Release Recovery Plan

**Goal:** Preserve the immutable failed-candidate v3.19.2-30 tag, repair its macOS/Linux conditional-compilation Clippy failure at the source, and publish the exact corrected commit as v3.19.2-31 only after three-platform CI succeeds.

## Constraints

- Do not delete, move, or reuse `v3.19.2-30`.
- Cancel its Release workflow before publication and verify no public v3.19.2-30 Release exists.
- Preserve every unfinished branch, dirty worktree, and user-owned untracked file.
- Do not suppress the diagnostic with a crate-wide `allow`; preserve the non-Windows read-only validation behavior.
- Do not create `v3.19.2-31` until CI for the exact main commit is green.

## Execution

- [x] Read the macOS job log and identify the exact target-specific unused binding.
- [x] Cancel the v3.19.2-30 CI and Release workflows.
- [x] Make non-Windows builds explicitly consume the verified projection repair result while retaining Windows/test repair data.
- [x] Change all four version sources to `3.19.2-31` and write cumulative release notes.
- [x] Run focused history tests plus local fmt, check, strict Clippy, diff, version, and UTF-8 gates.
- [ ] Commit and push main, then wait for Frontend, Windows, Ubuntu, and macOS CI success.
- [ ] Create and push annotated `v3.19.2-31` without force.
- [ ] Wait for every Release job, verify the public state, download all assets, and validate digests, signatures, URLs, and six updater platforms.
- [ ] Record publication evidence in `memory.md`, commit it after the tag, and push main without moving either tag.
