# CCSwitchMulti v3.20.1-1 Release Execution Record

## Candidate identity

- Migration branch: `bigstrongsun/upstream-v3.20.1-migration`
- Pre-versioning reviewed head: `82d7521d`
- Intended version/tag: `3.20.1-1` / `v3.20.1-1`
- Official audit boundary: `v3.20.2@f3b18df1`
- Disposition matrix: 154/154 non-merge commits classified
- Task 11 version-preparation commit: `a908c389`
- Candidate full-suite commit: `6b22a8d9`
- Superseded local build commit: `2f6dbb87`
- Final local gate and build commit: `9c573151`
- Main integration commit: `1cb3ebd4`
- Annotated tag peeled commit: pending

## Task 11 version preparation

- [x] Set `package.json` to `3.20.1-1`.
- [x] Set `src-tauri/tauri.conf.json` to `3.20.1-1`.
- [x] Set `src-tauri/Cargo.toml` to `3.20.1-1`.
- [x] Set the `cc-switch` package in `src-tauri/Cargo.lock` to `3.20.1-1`.
- [x] Add cumulative Chinese release notes with migration and downgrade boundaries.
- [x] Run Task 11 lightweight version, Cargo metadata, Markdown, diff and UTF-8 checks.
- [x] Commit Task 11 without invoking the release hook (`a908c389`).

## Task 12 concentrated local candidate gate

The complete gate is intentionally deferred until version preparation is committed. It must run with `LOCALAPPDATA` redirected to a dedicated temporary directory so Rust tests cannot write the real Claude Desktop configuration.

- [x] Freeze candidate product code at `6b22a8d9` and isolate `LOCALAPPDATA` at `C:\Users\sunda\AppData\Local\Temp\ccsm-v3.20.1-1-localappdata-110da36f`.
- [x] Full frontend Vitest: 185/185 files and 1537/1537 tests.
- [x] Full Rust library and integration tests: library 4047 passed, 0 failed, 6 ignored; all 12 integration binaries passed (125 tests total).
- [x] Frontend typecheck and CI-scope formatting.
- [x] `cargo check --all-targets`.
- [x] Strict Clippy at the repository CI scope (`cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings`).
- [x] `cargo fmt --check`.
- [x] Renderer production build.
- [x] Sidecar build after stream-fix integration.
- [x] Tauri release build and Windows NSIS export after stream-fix integration.
- [x] Final artifact size, SHA-256, signature, embedded version and updater metadata checks.

### Gate findings

- The first full frontend run at `a908c389` failed 1 of 1537 tests: the OpenCode PPIO preset exposed Tencent Token Plan models instead of its own DeepSeek V4 Flash model.
- Root cause: `1a905f16` inserted the Tencent OpenCode presets but also replaced the adjacent pre-existing PPIO `settingsConfig.models` object with a duplicate of the Tencent personal-plan catalogue. This was source-data contamination, not a shared-object runtime mutation.
- The existing PPIO assertion supplied the RED evidence. Restoring only the PPIO model object made the combined PPIO/Tencent focused suites pass 31/31. The full frontend item remains unchecked until it is rerun against the repaired candidate.
- The repaired candidate `110da36f` passed the full frontend suite: 185/185 files and 1537/1537 tests.
- The first parallel Rust full run compiled successfully, then passed 4045 tests, ignored 6 and failed 2. The Codex config CAS test passed alone, identifying process-wide test-home/settings interference between parallel tests rather than a product CAS failure. The final Rust suite will therefore use one test thread while Cargo compilation remains parallel.
- The other Rust failure was deterministic: `b30b470d` changed the Grok 4.5 cache-read seed from 0.50 to 0.30 but the historical backfill assertion still expected the old amount. Its existing RED now passes with cache cost `0.000075` and total cost `0.001575`.
- After the Grok expectation was corrected in `6b22a8d9`, the final serialized Rust library run passed 4047 tests, failed 0 and ignored 6. The integration binaries also passed in full: app_config_load 4/4, app_type_parse 2/2, deeplink_import 2/2, hermes_roundtrip 2/2, import_export_sync 26/26, mcp_commands 23/23, profile_roundtrip 8/8, provider_commands 10/10, provider_service 36/36, proxy_commands 2/2, skill_sync 7/7 and support_safety 3/3.
- The frontend full suite is not repeated after the Rust-only assertion correction. Remaining Task 12 checks are static analysis, renderer/sidecar/Tauri build and artifact validation.
- TypeScript, Prettier, `cargo check --all-targets`, rustfmt, diff and the renderer production build passed. The renderer emitted only the repository's existing stale browser-data, mixed static/dynamic import and large-chunk warnings.
- An exploratory stricter `cargo clippy --all-targets -- -D warnings` exposed 30 Rust 1.95 lints: 2 in production code and 28 in historical test code. The repository CI runs default-target Clippy, not all-targets. The two production findings were corrected with semantics-preserving standard-library forms (`contains` and `then_some`); the related provider-contract test passed 1/1 and the exact CI Clippy command then passed. The 28 test-only style findings are not hidden with `allow` attributes and are not folded into this release as an unrelated cross-module cleanup.
- `pnpm release:local` built from clean commit `2f6dbb87df0708c2e21defa7774cc5b326a8a932` and completed successfully. The feature-gated history-repair sidecar is 2,277,888 bytes with SHA-256 `839EAF944EC4E491353195C237F3F0E51C05AFD1ACDD93F74D0D9190FC573A0B`.
- The Windows export contains a 13,615,350-byte NSIS installer (`6C50102CBF754811AC8A59D74F7F0B03550B66AA1B0C4ACFAE0DCBB3D201A512`), a 16,095,636-byte portable ZIP (`26FEC584101BF1A20663E092AAE246027EF8A1B13FA2801798604327854184EB`) and a 43,657,728-byte raw executable (`2704A043DDF80E916615E6B25F479200C6AD614CE5623248D1A05E0DA0EBF99B`). The raw PE reports file/product version `3.20.1-1`; the ZIP contains the same-size `CCSwitchMulti.exe`.
- All 16 entries in exported `SHA256SUMS.txt` were recomputed and matched. `RELEASE-METADATA.md` identifies the exact branch, commit and version. The updater signature is non-empty (428 bytes) and exactly matches the Windows entry in `latest.json`; its version and GitHub tag URL are correct. Tauri CLI 2.10.1 does not expose a verify command and no standalone minisign verifier is installed, so independent public-key verification remains a GitHub updater-workflow gate rather than an unproven local claim.
- A post-build branch audit found one newly completed product branch absent from both the migration candidate and `main`: `bigstrongsun/codex-stream-retry-failed-terminal@c3537bc9`. Its parent `74547518` is already local `main`; only the stream-fix commit was transplanted onto the migration candidate as `e28f6c7a`, preserving main's parent-side history for the later merge. The integrated candidate passed `streaming_retry` 37/37 and the managed retry-budget regression 1/1 with isolated `LOCALAPPDATA`, plus `cargo check --all-targets`, CI-scope strict Clippy, rustfmt, diff and strict UTF-8 checks.
- The `2f6dbb87` artifacts above remain valid evidence for the pre-stream-fix product tree but are superseded for publication. They must not be uploaded as v3.20.1-1. A new clean build and new digests are required after `e28f6c7a`.
- Current official `origin/main` is exactly `v3.20.2@f3b18df1`; there are zero post-v3.20.2 commits. Other local branches remain excluded on previously documented grounds: power/commentary/portable are experiments or NO-GO, AgentMesh is an unintegrated prototype, Sub-Agent V2 is courseware, and the remaining stale branch tips are already semantically covered by the current main/migration architecture. Dirty worktrees and the main checkout's untracked `.tmp/` plus preview HTML are preserved untouched. One stream-fix worktree cannot be status-read under the current Windows SID due to Git dubious-ownership protection; no global safe-directory exception was added.
- The final clean rebuild completed from `9c5731516c1f6e69069e715966796f0ec16bc576`. The sidecar is 2,277,888 bytes with SHA-256 `9C9463DF8D7FCF70F84B8A80BDC3E8FC11A32B9AD9C26AD76EF6CBB1A4D63909`. The final Windows artifacts are: 13,623,498-byte NSIS `42BA45D04560CD3D413054FF7CC70ED92EFB60FF656C75636907C8813110EBB8`; 16,095,176-byte portable ZIP `9896412CE4A6AD4EDC15BF30C53DE62A110F964C631355A1B798A723DC7DE2E0`; and 43,656,192-byte raw EXE `4EC61CEFD0CF9972024CFEA281DC5BE5FF1535DE34046CC3B5F6F937E014E00A`.
- Final validation recomputed and matched all 16 checksum entries. The raw PE and sole portable ZIP executable both report/contain version `3.20.1-1`; `RELEASE-METADATA.md` identifies `9c573151`; the 428-byte signature exactly matches `latest.json`; updater version and tag URL are correct. The derived NSIS installed-executable hash is `CB45DE5539D472455629D6C122E76C3E18FEAA014799EF7052E80ACDCED654AC`. Independent public-key verification remains a GitHub workflow gate.

## Task 13 integration and publication

- [x] Re-audit worktrees and merge the verified branch into `main` (`1cb3ebd4`).
- [ ] Push the exact main commit and obtain green Windows, Ubuntu and macOS CI.
- [ ] Create and push annotated tag `v3.20.1-1` at the exact green commit.
- [ ] Verify every Release workflow job and public asset.
- [ ] Verify `latest.json`, six platform URLs, signatures and downloaded digests.
- [ ] Record installation/runtime evidence separately; no installation is implied by publication.

### Main integration evidence

- The merge had no product-code conflicts. The sole conflict was simultaneous top-of-file additions in `memory.md`; resolution preserved main's complete history-status record and every migration/stream/release record.
- The merged tree passed Codex preflight save 16/16, streaming retry 37/37, managed retry budget 1/1, consistency dialog 12/12, TypeScript, CI-scope strict Clippy, rustfmt, diff and strict UTF-8 checks. Full suites were not repeated because the merge added only previously focused-tested main/stream deltas to the already full-gated migration tree.
- Main checkout untracked `.tmp/` and `docs/provider-settings-layout-preview.html`, plus all unrelated dirty worktrees, remain unmodified and uncommitted.

## Guardrails

- Do not install, stop, restart or replace the live CCSwitchMulti before a recovery-capable install transaction and rollback inputs are verified.
- Do not touch the service on `127.0.0.1:15721` during candidate preparation.
- Do not silently absorb deferred official v3.20.2 work into this release.
- A failed gate is repaired at its root with a focused regression; only affected gates are rerun until the source settles, followed by one final complete local gate.
