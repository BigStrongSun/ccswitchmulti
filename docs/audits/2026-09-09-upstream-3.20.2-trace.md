# 2026-09-09 Official 3.20.2 Integration and Trace Repair

## Scope and Design

Baseline: CCSM `e69830b7`. Fetched official `upstream/main` and `v3.20.2` both resolve to `f3b18df12007d0fd79fd8ad8d310880664015197`; no post-tag commits.

Use semantic integration, not a whole-branch merge. Preserve CCSM Provider Set, account pool, dynamic metadata, router auth facade, user overrides and independent versioning. The previous 154-row audit is the starting inventory, not proof that every row was implemented.

Work on `bigstrongsun/upstream-3.20.2-trace` in the existing checkout to reuse local build caches. Preserve the two user-owned untracked paths. No installation, service restart, publication or push is authorized by this execution.

## Execution Checklist

- [x] Fetch the official boundary and inspect release notes and prior deferred rows.
- [x] Port missing reliability fixes with focused regression tests: URL masking, workflow journal filtering and updater error propagation.
- [x] Reconcile test isolation, image routes, translation keys and authentication against current CCSM ownership.
- [x] Review models endpoints, pricing, SoleAPI presets and pricing selector layout; explicitly record any remaining evidence limits.
- [x] Exercise trace requests with the production HTTP stack and retain the actual failure chain; historical failure reproduction is not claimed.
- [x] Repair trace diagnostics and reproduced response/UI defects; cover transport failures, response bounds, privacy and existing automatic-monitor semantics.
- [x] Run concentrated relevant Rust/frontend gates, type checking and formatting once the implementation stabilizes.
- [x] Update project memory and commit scoped changes; finish with the documented local main fast-forward and equal-tree verification.

## Evidence Policy

Built-in Web search returned no usable data in the initial lookup. Independent Matrix search for the exact release returned no results. Fetched Git objects and official repository release notes establish the upstream boundary; search indexing is not evidence that the release is absent. Vendor-specific runtime or pricing claims require their own evidence and must not be inferred from the release title.

The previous screenshot contains a first-stage reqwest send failure, not an IP geolocation or timezone application failure. A contemporaneous curl 200 does not reproduce the Rust client's transport or the historical failure. Preserve this distinction throughout diagnosis.

## Upstream Results

The release's broader Grok native Responses/tool-schema/subagent support, GPT-6 OAuth identity work, image generation/editing, DeepSeek MCP, Kimi schema, resumed usage, and Tencent/Qwen preset work largely predate this delta and already exist in CCSM. This is not a claim that every older audit row was adopted: previously deferred catalogue and UI rows retain their individual status.

Newly integrated:

- UTF-8 safe URL-mask truncation, journal-session exclusion, and real updater error propagation (`26c80ee6`). Each had a focused failing regression before repair.
- Novita's explicit model-directory override, plus structural exact-host fallback for saved Novita/JieKou Anthropic endpoints. Explicit user override remains authoritative; lookalike hosts are excluded. No wholesale JieKou preset import.
- Optional SoleAPI presets across nine apps, adapted to current per-app types. No partnership status, referral URL, sponsorship assets or verified-provider claim.
- Pricing selector width, verified using actual React components in a temporary browser harness: English and Japanese labels fit before the arrow; trace error text fits without horizontal overflow. Harness files, tab and Vite process were removed after QA.
- `requires_openai_auth` alignment only for single-provider placeholder takeover. File/default auth, ephemeral, keyring/auto/unknown stores and official/router/OAuth identities are distinguished. No keyring lookup or auth.json mutation. Scalar tokens require nonblank strings; malformed object/array tokens had a reproduced RED before repair.

Already covered: LOCALAPPDATA isolation in integration support, Pi translations and the stronger Images edits routing.

Remaining exception: `ccc140a2` September price changes are deferred. Matrix's vendor pricing page did not expose rate tables; current prices and promotional expiry remain **insufficiently evidenced**. No pricing seed/repair was changed. SoleAPI docs search found a page whose open returned 404; direct vendor endpoint checks failed TLS negotiation. Preset source contracts are tested, live vendor availability is not established.

## Trace Root-Cause Boundaries

The screenshot's generic string discarded reqwest's source chain. It establishes the failed stage (`send` to ChatGPT trace), but cannot identify historical DNS, TCP, TLS or timeout cause. The production Rust first-stage request returned HTTP 200 in 478 ms during this investigation; the historical failure did not reproduce.

Reproduced code defects repaired:

1. Transport errors now retain the redacted source chain, failure category and proxy-path classification. Geolocation URLs, observed IP in request URLs and credentials are not included.
2. The previous 64 KiB limit ran after full-body allocation. Content-Length and cumulative streamed chunks now enforce the bound during reading, including exact-limit/no-length cases.
3. Diagnostic DNS has a two-second bound and starts alongside HTTP. The join still allows up to two seconds before trace-body consumption; this bounded P3 tradeoff is explicit, not advertised as zero-delay DNS.
4. Enabling automatic detection now surfaces a returned monitor error. Starting a new manual detection clears the previous report so a failed refresh cannot leave an old Apply action.

No proxy routing, TLS verification, default timeout, retry policy or automatic monitor backoff was weakened. No arbitrary fallback host was introduced.

## Review and Verification

One independent reviewer performed two scoped passes. No P0-P2 findings remained; the malformed-token P3 was addressed and stream-boundary coverage added. The bounded DNS latency remains as documented above.

- Frontend final gate: 188 files, 1557 tests passed. Existing React act warnings remain.
- TypeScript and renderer production build passed. Existing browser-data age, mixed dynamic import and large-chunk warnings remain.
- Changed frontend files passed Prettier; diff whitespace check passed.
- Rust final gate (`--lib --tests`, isolated LOCALAPPDATA, one test thread): 4062 library tests passed, zero failed, seven ignored; 125 integration tests passed. This includes the corrected auth identity fixtures and malformed-token RED-to-GREEN regression.
- Strict UTF-8 decoding, no BOM and no replacement character checks passed on all 21 changed/new task files.
- Whole-pipeline diagnostic: the production detection function in the test harness completed trace plus IP geolocation in 2.900 seconds, `system_or_transparent` path. No raw IP or credentials were printed. This is source-stack verification, not invocation of the installed executable.
- Strict CI-style default-target Clippy (`-D warnings`) passed. No full suite was repeated after the successful final source gates.

Source version remains `3.20.1-3`. This task does not build an installer, publish, install or restart the running `3.19.2-31` executable. Source validation must not be presented as installed-runtime validation.

## Local Commit Inventory

- `26c80ee6`: upstream reliability corrections.
- `662076eb`: upstream model discovery, optional presets, layout and auth-ownership rewrite.
- `2293a637`: trace diagnostics, response bounds and UI state repair.

The final documentation commit records the audit and project memory. Local main integration is a fast-forward from the unchanged `e69830b7` baseline; no upstream ancestry merge, push, tag, release or installation is implied.
