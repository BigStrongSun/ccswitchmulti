# CCSwitchMulti v3.20.2-19 发布审计

日期：2026-09-18（Asia/Shanghai）

## 发布内容与来源

- 本版唯一功能改动：DeepSeek 角色 slug 别名统一修复（提交 `7fb839db4d9c7e97e4d70ccf4f4154eb400bcf73`），修复 V2 Agent 配置页 `deepseek-flash` 角色徽章误报“目录中缺失”、flash 角色 toml 不生成、profile 退化成 disabled stub 等问题。根因与验证详见 `memory-2026-09-18-deepseek-role-slug-alias-fix.md`。
- 发布仓库：`BigStrongSun/ccswitchmulti`，远程为 `fork`；未触碰官方 upstream。
- 发布前先把 `fork/main`（`c95761b7`，含 v3.20.2-18 审计与 watchdog rustfmt）合入 main（merge `5b67299b32a06589af0bf7f57af2635d31950ed9`，`memory.md` 冲突保留双方条目）。
- 收尾提交：`13d1722d118f00c95a729ab9317794e0dfaa3d6a`（`release: prepare CCSwitchMulti v3.20.2-19`），只提升版本号（package.json / Cargo.toml / Cargo.lock / tauri.conf.json 由 `3.20.2-18` → `3.20.2-19`）并新增中英文发布说明，无代码行为改动。
- 正式发布 tag：`v3.20.2-19`，annotated tag 固定指向 `13d1722d118f00c95a729ab9317794e0dfaa3d6a`；tag 不移动、不复用。
- 发布前 main、fork/main、tag 目标三者一致指向 `13d1722d`。

## GitHub Actions 与 Release

- Release workflow：`35287539781`（事件 `push`，`head_branch=v3.20.2-19`，`head_sha=13d1722d118f00c95a729ab9317794e0dfaa3d6a`），`2026-09-17T23:35:44Z` 触发，最终 `completed / success`。
- 五个平台构建全部 success：`release (ubuntu-22.04)`、`release (ubuntu-22.04-arm, arm64)`、`release (windows-2022)`、`release (windows-2022, arm64)`、`release (macos-14)`；`macos-14` 为最长（v3.20.2-18 同名 job 历时 68.5 分钟，本版量级相当）。
- `Publish GitHub Release` 与 `Assemble latest.json` 全部 success。
- 正式 Release：[CCSwitchMulti v3.20.2-19](https://github.com/BigStrongSun/ccswitchmulti/releases/tag/v3.20.2-19)，`draft=false`、`prerelease=false`，于 `2026-09-18T00:45:31Z` 发布，共 19 个资产（与 v3.20.2-18 一致）。

## 资产与 manifest

19 个资产：
- Linux arm64：`AppImage`(93,833,736)、`AppImage.sig`(436)、`deb`(17,558,004)、`rpm`(17,565,137)
- Linux x86_64：`AppImage`(96,401,912)、`AppImage.sig`(432)、`deb`(18,320,940)、`rpm`(18,322,804)
- macOS：`dmg`(37,273,509)、`tar.gz`(37,930,709)、`tar.gz.sig`(412)、`zip`(36,950,557)
- Windows arm64：`Portable.zip`(16,059,555)、`Setup.exe`(12,424,286)、`Setup.exe.sig`(432)
- Windows x86_64：`Portable.zip`(16,839,282)、`Setup.exe`(13,796,195)、`Setup.exe.sig`(432)
- `latest.json`(3,696)

`latest.json`（下载后解析）：`version=3.20.2-19`，含 `darwin-aarch64`、`darwin-x86_64`、`windows-x86_64`、`windows-aarch64`、`linux-x86_64`、`linux-aarch64` 六个平台；六个 URL 均指向 `v3.20.2-19` 资产，六个 signature 均非空（对应各自 `.sig` 资产）。

## SHA-256 抽查

- `CCSwitchMulti-v3.20.2-19-Windows-Setup.exe`：下载 13,796,195 字节，本地计算 SHA-256 `022d71a7ffda82c76f7aa12ef4aeae6a549343e4d7571d5ed7080b7b0e5f136d`，与 GitHub 资产 `digest`（`sha256:022d71a7...136d`）逐项一致，`mismatch=0`。

## 本机运行态边界

- 按用户指示“先不用管本地替换”，本版**未做本机安装、重启或运行态验证**。
- 本机安装态二进制仍为 `v3.20.2-18`，**不包含** `7fb839db` 的 DeepSeek 修复；要让修复在本机生效，需构建/安装 `v3.20.2-19` 后重启 Codex Desktop/app-server（源码/测试/release 证据与本机运行态证据仍相互独立）。

## CI 与测试状态的诚实边界

- 本次 push main 触发的 main CI `35287533992`（`head_sha=13d1722d`）`completed / failure`，失败 job：`Frontend Checks` + `Backend Checks (macos-latest / windows-latest / ubuntu-22.04)`。
- **关键对照**：本次 push 之前、`fork/main` 上的 main CI `34960285771`（`head_sha=c95761b7`）同样 `failure`，失败 job 与上面**完全相同**（Frontend + 三平台 Backend）。两版失败集一致 → 这些失败**先于本次 DeepSeek 修复存在**，不是 `7fb839db` 引入的新回归。
- 局限：未认证 GitHub API 的 job `/logs` 端点返回 403（需带 `actions:read` 的 PAT），无法读取失败明细文本；上条结论依据“同一 job 失败集在 push 前后一致”+ v3.20.2-18 审计记录的既有失败（Frontend `AddProviderDialog.test.tsx` mock 未接入 `restore_codex_provider_protocol_evidence` + `tauri.local` DNS `EAI_AGAIN` 未处理 rejection；三平台 Clippy `too_many_arguments`/`type_complexity` 等）。
- 本修复的聚焦测试全部通过（与 release 构建无关的本地证据）：Rust `codex_subagent_profiles::` 97、`codex_config::` 232、`codex_multirouter::` 104、`deepseek` 过滤 45；前端 92 passed；`pnpm typecheck` 通过。
- 结论：v3.20.2-19 Release workflow 的五平台构建、资产上传与 `latest.json` 汇总不受 main CI 既有失败影响；但 v3.20.2-19 **不能宣称“main CI 全绿”**。

## 仓库并发状态备注

- 发布收尾期间，主 worktree 已被切到特性分支 `bigstrongsun/te-provider-type`（基于 main `13d1722d`，含提交 `4cd100ef` `feat(provider): 新增一等 TE Provider 类型（Token Exchange）` 及未提交的 `protocol_compatibility.rs` 改动）。
- 该分支与 main、release tag 相互独立；本次发布全部在 main 上完成，本审计通过临时 worktree 落到 main，未改动该特性分支的工作区。
