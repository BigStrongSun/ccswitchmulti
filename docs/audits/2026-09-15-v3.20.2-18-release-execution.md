# CCSwitchMulti v3.20.2-18 发布审计

日期：2026-09-15（Asia/Shanghai）

## 发布来源与 tag

- 发布仓库：`BigStrongSun/ccswitchmulti`，远程为 `fork`；未触碰官方 upstream。
- `fork/main` 在本次收尾提交 `aff5fa4668fe94b7eb7497cd27ccff1226663b6b`。
- 正式发布 tag：`v3.20.2-18`，annotated tag 固定指向 `aba962bbebc53423ef91e0e08b03524021e916d8`。
- tag 不包含收尾审计提交；tag 不移动、不复用。收尾提交只补 Rustfmt 与本审计记录，不改变已发布运行逻辑。

## GitHub Actions 与 Release

- Release workflow：`34953579867`（`head_sha=aba962bbebc53423ef91e0e08b03524021e916d8`）。
- 五个平台构建全部 success：Windows x64、Windows ARM64、Linux x64、Linux ARM64、macOS universal。
- `Publish GitHub Release` 与 `Assemble latest.json` 全部 success。
- 正式 Release：[CCSwitchMulti v3.20.2-18](https://github.com/BigStrongSun/ccswitchmulti/releases/tag/v3.20.2-18)，`draft=false`、`prerelease=false`，于 `2026-09-15T10:47:15Z` 发布，共 19 个资产。
- Release 资产包括五个平台分发包、对应 updater 签名及 `latest.json`；GitHub 记录的资产摘要已在下载后逐项复算，抽查集合 `mismatch_count=0`。

## updater manifest 与签名

- 下载目录：`C:\Users\sunda\Documents\LLMservice\ccswitchmulti-v3.20.2-18-github-verify-20260915-a`。
- `latest.json` SHA-256：`0B8386294DE4A3D0E21C3C9C216AD523692D09380D093E0446AFB4482A6D76B9`。
- manifest `version=3.20.2-18`，包含 `darwin-aarch64`、`darwin-x86_64`、`windows-x86_64`、`windows-aarch64`、`linux-x86_64`、`linux-aarch64` 六个平台；六个 URL 均指向 `v3.20.2-18`，六个 signature 均非空并对应各自 `.sig` 资产。
- GitHub Windows x64 安装包：`13,792,620` bytes，SHA-256 `CA6D8975CEDFE85EC65BFC389B9CE082932E1B8EDAB8F6E8286EB45D2D43563D`。

## 本机运行态边界

- 本机当前监听 `127.0.0.1:15721`，PID `44308`，`listener_role=takeover`。
- `/health` 返回 healthy（HTTP 200）；`/status` 返回 `version=3.20.2-18`、`running=true`、端口 15721。
- 当前运行文件：`C:\Users\sunda\AppData\Local\CCSwitchMulti\cc-switch.exe`，FileVersion/ProductVersion 均为 `3.20.2-18`；运行文件 SHA-256 `59C0BB6D60EA4056D2875BE416375E4E1D0DADC40ADDD38F1D322CD16F53E777`。
- 本机事务构建安装包另存于 `C:\Users\sunda\Documents\LLMservice\ccswitchmulti-v3.20.2-18-local`，本地 NSIS 安装包 SHA-256 `F6414FFDF036ED5D866CB41DD5787A6AD8BBC81985C0EAE13373265487F63E4F`。本地构建包与 GitHub runner 构建包不是同一文件，不能用 hash 相等替代版本/运行态核验。

## CI 与测试状态的诚实边界

- 远端 main CI：`34953918818` 失败。Frontend job 的单元测试为 `1613 passed / 1 failed`，失败为 `tests/components/AddProviderDialog.test.tsx` 的 mock 未接入 `restore_codex_provider_protocol_evidence`，并伴随 `tauri.local` 的 DNS `EAI_AGAIN` 未处理 rejection；后端三平台 Clippy 仍有 `too_many_arguments`、`type_complexity` 等错误。
- 这些 CI 失败不影响本次 Release workflow 的五平台构建、资产上传和 `latest.json` 汇总，但 v3.20.2-18 不能宣称“main CI 全绿”。AddProviderDialog 测试夹具与 Clippy 清理应作为后续独立提交处理，不能移动或改写 v3.20.2-18 tag。
- 本地分页历史 Rust 测试为 29 passed / 1 ignored，前端回归 16/16，看门狗测试 2/2；TypeScript、Prettier、Rustfmt 检查通过。

## 搜索与交叉验证

- Codex 内置 WebSearch 独立查询 GitHub Release、仓库 release 约定及 GitHub Actions 权限文档。
- Matrix WebSearch 独立查询并直读 `v3.20.2-18` GitHub Release 页面；其页面文本确认 tag、发布日期及资产区块，资产细节以 GitHub API/CLI 实时结果为准。
- 两条网页搜索链均未替代本地源码、Git refs、Actions job、下载 hash 和运行态证据；发布结论以这些一手实时证据裁决。
