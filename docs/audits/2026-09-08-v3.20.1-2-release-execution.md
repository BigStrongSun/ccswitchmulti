# CCSwitchMulti v3.20.1-2 Release Execution Record

## Candidate identity

- Base release: `v3.20.1-1@325d96ed`
- Pre-versioning main commit: `5eaf58e0`
- Intended version/tag: `3.20.1-2` / `v3.20.1-2`
- Official audit boundary: `v3.20.2@f3b18df1`
- Included product fix: `5eaf58e0` (`fix(codex-oauth): distinguish identity upgrade from expiry`)
- Version-preparation commit: `d8404bb3`
- Final local gate/build commit: pending
- Pre-tag CI run: pending
- Annotated tag object and peeled commit: pending
- Release workflow run: pending

## Release preparation

- [x] Fetch all remotes and tags with pruning.
- [x] Confirm `fork/main` exactly matched local `main` before integration.
- [x] Confirm official `origin/main` and `upstream/main` still equal `v3.20.2@f3b18df1` with no later official commits.
- [x] Re-audit non-merged branches and identify `bigstrongsun/fix-oauth-status-refresh@5eaf58e0` as the only new completed product branch for this release.
- [x] Fast-forward the verified OAuth branch into local `main` without touching unrelated untracked files or worktrees.
- [x] Set the four version sources to `3.20.1-2`.
- [x] Add Chinese release notes with the exact OAuth, protocol-probe and runtime boundaries.
- [x] Commit the version-preparation state without invoking the automatic post-commit release hook.

## Concentrated local gate and build

- [x] Full frontend Vitest: 186/186 files and 1541/1541 tests with four bounded workers.
- [x] Full Rust library and integration tests with isolated `LOCALAPPDATA`, `TEMP`, `TMP` and `--test-threads=1`: 4051 library tests passed, 6 ignored, and 125 integration tests passed.
- [x] TypeScript, renderer production build, Cargo check, CI-scope Clippy and rustfmt.
- [x] Diff, JSON and strict UTF-8/no-BOM/no-U+FFFD checks for the release delta.
- [ ] Run one clean `pnpm release:local` build from the committed candidate.
- [ ] Validate Windows sidecar, NSIS, portable ZIP, raw EXE, checksums, embedded version, signature consistency and updater metadata.

## Publication

- [ ] Push the exact candidate commit to `fork/main`.
- [ ] Wait for Frontend, Windows, Ubuntu and macOS CI to pass.
- [ ] Create and push annotated tag `v3.20.1-2` at that exact green commit.
- [ ] Wait for every Release workflow job to pass.
- [ ] Verify the public Release is stable, latest and contains the complete expected asset set.
- [ ] Download public assets and verify GitHub metadata, SHA-256, `latest.json`, all six updater targets, URLs and signatures.
- [ ] Check R2 synchronization separately; do not infer it from GitHub Release success.

## Guardrails

- Do not reuse any `v3.20.1-1` artifact for this release.
- Do not install, stop, restart or replace the live CCSwitchMulti process during candidate preparation or publication.
- Do not touch the service on `127.0.0.1:15721`.
- Do not absorb unrelated experimental, deferred or dirty-worktree changes.
- Repair failed gates at their root, rerun affected checks only while source changes, then perform one final complete candidate gate.

## Local gate notes

- The first Vitest invocation discovered 26 foreign files under the ignored `.local/CCSwitchMulti-Task6/.../.codex/plugins` snapshot in addition to the repository's 186 suites. The repository suites themselves passed 1541/1541; the foreign files failed because they were not Vitest suites or depended on Playwright packages outside this project.
- Root cause was an incomplete discovery boundary: `vitest.config.ts` excluded `.worktrees`, `.tmp`, `node_modules` and `dist`, but not the repository-local task snapshot root `.local`. The permanent test-runner fix adds `.local` to the same evidence/artifact exclusion list. The next complete run discovered exactly 186 files and passed all 1541 tests.
- Prettier then identified only the newly added `SubscriptionQuotaFooter.test.tsx` as unformatted. It was mechanically formatted; the affected file passed 2/2, and the repository-wide format check passed. Because this changed only test formatting after the complete run, the 1541-test suite was not repeated.
- The renderer build retained only existing stale browser-data, mixed static/dynamic import and large-chunk warnings. No source, assertion, compiler, Clippy or build failure remains in the concentrated local gate.
