# 2026-09-18 CCSwitchMulti v3.20.2-19 正式发布

## 结论

- v3.20.2-19 已正式发布到 `BigStrongSun/ccswitchmulti`（远程 `fork`）。
- annotated tag `v3.20.2-19` 固定指向 `13d1722d`（`release: prepare CCSwitchMulti v3.20.2-19`）。
- Release workflow `35287539781`（五平台构建 + Publish + Assemble）全部 success。
- GitHub Release `draft=false`、`prerelease=false`，19 个资产；`latest.json` `version=3.20.2-19`、六平台/六签名齐全。
- Windows-Setup.exe SHA-256 与 GitHub digest 一致（mismatch=0）。详见 `docs/audits/2026-09-18-v3.20.2-19-release-execution.md`。

## 本版唯一功能改动

- DeepSeek 角色 slug 别名统一修复 `7fb839db`（“目录中缺失”误报 + flash 角色未生成）。根因/验证详见
  `memory-2026-09-18-deepseek-role-slug-alias-fix.md`。

## 运行态边界（重要）

- 按用户指示“先不用管本地替换”，**本机未安装、未重启、未做运行态验证**。
- 本机安装态二进制**仍为 v3.20.2-18，不含 `7fb839db` 修复**；要让修复在本机生效需构建/安装 v3.20.2-19 后重启。
- 源码/测试/release 证据 与 本机运行态证据 相互独立，不能互相替代。

## CI 诚实边界

- main CI `35287533992`（head `13d1722d`）failure：Frontend Checks + 三平台 Backend Checks。
- push 前的 main CI `34960285771`（head `c95761b7`）失败 job 集合**完全相同** → 既有失败，非 `7fb839db` 引入。
- 未认证 API 无法读 job /logs（403，需 actions:read PAT），明细以 v3.20.2-18 审计记录为准
  （Frontend AddProviderDialog mock + tauri.local DNS EAI_AGAIN；三平台 Clippy）。
- v3.20.2-19 不能宣称“main CI 全绿”。

## 仓库并发备注

- 收尾期间主 worktree 被切到 `bigstrongsun/te-provider-type`（基于 main `13d1722d`，提交 `4cd100ef`
  “feat(provider): 新增一等 TE Provider 类型（Token Exchange）” + 未提交 `protocol_compatibility.rs`）。
- 与本 release 相互独立；本审计经临时 worktree 落到 main，未动该特性分支工作区。
